//#include "BotEvent.h"

/*bool CBotEvent::executeRoll(float currentTime)
{
	if (currentTime >= m_lastRollTime)
	{
		m_target->roll();
		m_lastRollTime = (currentTime + 5.f);
		return true;
	}
	return false;
}*/